<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_partner_form_inherit_bio" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.bio</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="priority" eval="130"/>
        <field name="arch" type="xml">
            <!-- ODOO-469 begin -->
            <xpath expr="//group[@name='accounting_entries']" position="inside">
                <field name="bio_cash_flow_analytic_account_id"/>
            </xpath>
            <!-- ODOO-469 end -->
        </field>
    </record>

</odoo>